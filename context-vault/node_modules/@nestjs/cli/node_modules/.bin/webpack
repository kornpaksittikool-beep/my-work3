#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
basedir_win="$basedir"
exe=""
msys=""

case `uname -a` in
  *CYGWIN*|*MINGW*|*MSYS*)
    if command -v cygpath > /dev/null 2>&1; then
      basedir_win=`cygpath -w "$basedir"`
    fi
    exe=".exe"
    msys="true"
  ;;
  *WSL2*)
    if command -v wslpath > /dev/null 2>&1; then
      basedir_win="$(wslpath -w "$basedir" 2> /dev/null)"
      if [ $? -ne 0 ] || [ -z "$basedir_win" ]; then
        basedir_win="$basedir"
      else
        exe=".exe"
      fi
    fi
  ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/mnt/d/my-work-3/node_modules/.pnpm/webpack@5.97.1_esbuild@0.27_da256cfde0d1acfd114f6131d5505612/node_modules/webpack/node_modules:/mnt/d/my-work-3/node_modules/.pnpm/webpack@5.97.1_esbuild@0.27_da256cfde0d1acfd114f6131d5505612/node_modules:/mnt/d/my-work-3/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/mnt/d/my-work-3/node_modules/.pnpm/webpack@5.97.1_esbuild@0.27_da256cfde0d1acfd114f6131d5505612/node_modules/webpack/node_modules:/mnt/d/my-work-3/node_modules/.pnpm/webpack@5.97.1_esbuild@0.27_da256cfde0d1acfd114f6131d5505612/node_modules:/mnt/d/my-work-3/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -n "$exe" ] && [ -x "$basedir/node.exe" ]; then
  exec "$basedir/node.exe"  "$basedir_win/../../../../webpack/bin/webpack.js" "$@"
elif [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../webpack/bin/webpack.js" "$@"
elif command -v node >/dev/null 2>&1; then
  exec node  "$basedir/../../../../webpack/bin/webpack.js" "$@"
elif [ -n "$exe" ] && command -v node.exe >/dev/null 2>&1; then
  exec node.exe  "$basedir_win/../../../../webpack/bin/webpack.js" "$@"
else
  exec node  "$basedir/../../../../webpack/bin/webpack.js" "$@"
fi
# cmd-shim-target=D:/my-work-3/node_modules/.pnpm/@nestjs+cli@10.4.9_esbuild@_fde0942b2a786489691fcc35a8d692c3/node_modules/webpack/bin/webpack.js
